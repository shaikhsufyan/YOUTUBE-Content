// Template Literals ::

 
// Template literal are use with (``),  
// If we have to use variable use ${}.
// Template literals come in ECMA script

let a = 10; 
let b = 20;
console.log("sum of "+ a +" and "+ b +" is " + (a+b));
console.log(`sum of ${a} and ${b} is ${a+b}`);
  

 
 